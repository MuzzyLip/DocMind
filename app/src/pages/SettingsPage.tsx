import React, { ReactElement } from "react";

interface SettingsPageProps {}

const SettingsPage: React.FC<SettingsPageProps> = (): ReactElement => {
    return <div>SettingsPage</div>;
};

export default SettingsPage;
