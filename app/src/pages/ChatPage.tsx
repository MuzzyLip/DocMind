import React, { ReactElement } from "react";

interface ChatPageProps {}

const ChatPage: React.FC<ChatPageProps> = (): ReactElement => {
    return <div>ChatPage</div>;
};

export default ChatPage;
