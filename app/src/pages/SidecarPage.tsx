import React, { ReactElement } from "react";

interface SidecarPageProps {}

const SidecarPage: React.FC<SidecarPageProps> = (): ReactElement => {
    return <div>SidecarPage</div>;
};

export default SidecarPage;
